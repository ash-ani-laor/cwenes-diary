import type { Meta, StoryObj } from '@storybook/react'

import DivinationsPage from './DivinationsPage'

const meta: Meta<typeof DivinationsPage> = {
  component: DivinationsPage,
}

export default meta

type Story = StoryObj<typeof DivinationsPage>

export const Primary: Story = {}
